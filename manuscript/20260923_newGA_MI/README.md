# 20260923 — manuscript results regenerated with the current GA/MI runs

All data-dependent tables and figures of the manuscript, rebuilt from the current
run families. **No Word file was modified**; paste these tables/figures into the
draft when ready.

## Current run families

| Method | Workspace dir | Config |
|---|---|---|
| GA | `loo_r0.7_{Scaler}_maxk4_noMI_inner1` | 5 GA folds x **4** random seeds, inner fitness = **single** stratified holdout (1 AUC/individual/generation), `USE_MI_INITIALIZATION=False`, max k=4 |
| MI | `mi_loo_r0.7_{Scaler}_inner1` | 5 folds x **4** random seeds, top-k=4 |

Generated by `cv_pipeline_loo_svm_from_claude_web.py` (GA) and
`cv_pipeline_mi_loo_svm_from_claude_web.py` (MI).

Previous (superseded) runs behind the 20260915 draft:
GA `loo_r0.7_{Scaler}_noMIinit` (8 seeds, 3 inner folds) and MI
`mi_loo_r0.7_{Scaler}` (8 seeds).

## Representative configurations

- **GA peak:** RobustScaler, BW10 → pooled AUC **0.843** (0.746–0.925)
- **MI peak:** StandardScaler, **BW20** → pooled AUC **0.778** (0.668–0.872)
  (the old MI peak was SS BW25 = 0.779; with the current runs SS BW25 = 0.756,
  so the MI-optimal StandardScaler bin width moves 25 → **20**).

Change `SS_BW` / `MATCHED` in `ga_mi_config.py` to regenerate everything at a
different StandardScaler bin width.

## Files

| File | Manuscript item |
|---|---|
| `verification_old_vs_new.md` (+ `.csv`) | correctness check vs the previous runs |
| `table2_pooled_auc.md` (+ `.csv`) | Table 2 — pooled AUC, 10 BW x 3 scalers, GA vs MI |
| `figure2_pooled_auc_heatmap.png` | Figure 2 — heatmap of Table 2 |
| `table3_features_ss20.md` (+ `.csv`) | Table 3 — features at MI-optimal (SS BW20) |
| `table4_features_rs10.md` (+ `.csv`) | Table 4 — features at GA-optimal (RS BW10) |
| `table5_diagnostic.md` (+ `.csv`) | Table 5 — diagnostic performance (RS BW10, SS BW20) |
| `table6_reference_models.md` (+ `.csv`) | Table 6 — reference models vs GA/MI |
| `table6_delong_companion.md` (+ `.csv`) | DeLong contrasts underlying Table 6 |
| `tableS1_ga_triad_stability.md` (+ `.csv`) | Table S1 — GA shape-triad stability |
| `tableS2_ga_bw10_rs_vs_pt.md` (+ `.csv`) | Table S2 — GA BW10 RobustScaler vs PowerTransformer |
| `figure3_roc.png` | Figure 3 — ROC radiomics + clinical |
| `figure4_calibration_dca.png` | Figure 4 — calibration + decision curve analysis |

Unchanged (no GA/MI dependency): Table 1 (demographics), Figure 1 (flowchart),
the fixed-triad / univariate-MWU reference values, and the clinical-model curves
(loaded from `manuscript/20260906/`).

## Verification (new = 4 seeds vs old = 8 seeds)

- MI feature rankings are near-identical (Spearman ρ ≈ 0.99, top-4 Jaccard 0.78–1.00);
  mean ΔAUC within ±0.015.
- GA feature rankings ρ ≈ 0.96–0.99; mean ΔAUC +0.006 to +0.015 (slightly higher).
- Delong significant differences are confined to a few bin widths (GA SS/PT and PT/RS),
  all in the direction of the new run. Full detail in `verification_old_vs_new.md`.

## Flag — the MI-optimal configuration changed

Because MI's peak moved to SS BW20, the matched StandardScaler pair is now at BW20:

| Config | GA | MI | DeLong p |
|---|---:|---:|---:|
| RS BW10 (GA peak) | **0.843** | 0.706 | 0.005 (**) |
| SS BW20 (MI peak)  | 0.757 | **0.778** | 0.586 (n.s.) |

At the MI-optimal SS BW20 the two methods are statistically indistinguishable and
MI is nominally higher; at GA's RS BW10, GA is significantly higher. If the
previous matched config (SS BW25, where GA = 0.801, MI = 0.756) is preferred for
continuity with the 20260915 draft, set `SS_BW = 25` in `ga_mi_config.py` and rerun
the four scripts.

Calibration/DCA: GA RS BW10 remains best (Brier 0.160, AUCD 0.235); at SS BW20 the
two methods are close (GA Brier 0.205/AUCD 0.188; MI 0.193/0.192).

*Generated 2026-09-23.*
